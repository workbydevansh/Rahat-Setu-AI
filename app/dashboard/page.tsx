import { DashboardHub } from "@/components/DashboardHub";

export const metadata = {
  title: "Dashboard",
};

export default function DashboardPage() {
  return <DashboardHub variant="dashboard" />;
}
